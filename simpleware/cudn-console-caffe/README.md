# build
```
docker build -t simpleware/console-caffe-base:1 -t simpleware/console-caffe-base:latest -f ./caffe-base.Dockerfile .
docker build -t simpleware/console-caffe-compilse:1 -t simpleware/console-caffe-compilse:latest -f ./caffe-compilse.Dockerfile .
```


